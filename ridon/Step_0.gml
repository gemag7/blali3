/// @description Insert description here
// You can write your code in this editor



if (place_meeting(x + hspeed, y, block))
{
	while(!place_meeting(x + sign(hspeed), y, block))
	{
	x += sign(hspeed);
	}

hspeed = 0;
}

if (place_meeting(x, y + vspeed, block))
{
	while(!place_meeting(x, y + sign(vspeed), block))
	{
	y += sign(vspeed);
	}

vspeed = 0;
}

if (place_meeting(x + hspeed, y + vspeed, block))
{
	hspeed = 0;
	vspeed = 0;
}


if place_meeting(x,y+abs(spieler.vspeed),spieler)&&spieler.vspeed<0
schaden(32,1,1)


if verletzt
image_alpha = cos(4*current_time)
if kp<=0
{
//	audio_pause_all()

if redet{
	show_message("Ridon: ich habe bereits geahnt dass du mich besiegen würdest")
	show_message("Ridon: deswegen habe ich die Techniken meines Vorgängers studiert")
	show_message("Ridon: ich habe meine Kreationen die du besiegt hast aufbewahrt für diesen Moment")
redet = false	
}
if !instance_exists(markokopk)
{
	instance_create_depth(0,0,0,jeffkopk)
	instance_create_depth(0,50,0,lordddddi)
	instance_create_depth(0,100,0,markokopk)
}
if zuziehen = 3
room_goto_next()
}
if aktiv{
jagen(2,ziel1)
}