/// @description Insert description here
// You can write your code in this editor
if !visible{
if hudleben.deineMudda = krimhiphase1
{
	visible = true
	with explo1{
		if !visible
		alarm[0] = 24
		visible = true
	}
	with other instance_destroy()
}

}
else{
	var gestossen = choose(true,false)
	if gestossen{
		if !instance_exists(lordskugel)
		y+= random(400)
		image_alpha = choose(1,( cos(4*current_time)))
with other instance_destroy()
}
}